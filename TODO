 - client-side test on coffee-script
 - client-side scripts on coffee
 - implement tasks
   test-watch
   + debug(nodev, node-inspector)
	  - node-inspector should run exactly after node server started

 Feautures to implement:
 - building and deploy app locally(cluster, nginx)
 - support for less
